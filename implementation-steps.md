# PHP 代码诊断 RAgent 分阶段实施步骤

## 1. 项目目标

实现一个面向 PHP/Miyou 项目的异步代码诊断 Agent：用户输入业务问题，系统自动检索代码、配置、调用链、数据库和日志，并输出带证据链的诊断报告。

第一版只读访问代码、数据库和日志，不允许自动修改代码、数据或线上配置。

## 2. 总体执行流程

```text
用户提问
  ↓
创建诊断任务
  ↓
Agent 理解问题并生成检索计划
  ↓
搜索代码、配置和调用关系
  ↓
必要时验证数据库和日志
  ↓
生成带证据的诊断报告
  ↓
保存任务和证据
```

## 3. 阶段总览

| 阶段 | 名称 | 主要结果 |
|---|---|---|
| 0 | 环境初始化 | 项目可以启动、测试和部署 |
| 1 | 任务闭环 | 可以创建、执行、查询和取消诊断任务 |
| 2 | 代码检索 MVP | 可以搜索 PHP 代码并生成带行号的报告 |
| 3 | 代码结构理解 | 可以分析符号、引用和调用链 |
| 4 | 事实验证 | 可以只读查询数据库、配置和日志 |
| 5 | 知识库增强 | 可以复用历史问题和业务文档 |
| 6 | 前端协作 | 可以查看实时进度和交互式报告 |
| 7 | 生产上线 | 完成权限、安全、性能和监控 |

---

## 4. 阶段 0：环境初始化

### 目标

建立 FastAPI、Worker、Redis 和 PostgreSQL 的基础运行环境。

### 实施步骤

1. 创建 Python 项目和虚拟环境。
2. 加入 FastAPI、Redis 客户端、任务队列、PostgreSQL 驱动和数据库迁移工具。
3. 创建配置管理，区分开发、测试和生产环境。
4. 创建 Docker Compose，包含 API、Worker、Redis 和 PostgreSQL。
5. 创建 `/health` 健康检查接口。
6. 配置结构化日志。
7. 配置代码格式化、静态检查和单元测试命令。
8. 建立数据库迁移目录和基础 README。

### 建议目录

```text
app/
├── api/
├── agent/
├── tools/
├── workers/
├── indexer/
├── models/
├── repositories/
└── settings.py
tests/
migrations/
scripts/
docker-compose.yml
pyproject.toml
README.md
```

### 产出与验收

- API 和 Worker 可以独立启动；
- API 可以连接 Redis 和 PostgreSQL；
- `/health` 能反映依赖服务状态；
- 空项目可以执行测试命令；
- 配置中不能写入真实密钥。

---

## 5. 阶段 1：异步任务闭环

### 目标

实现“创建任务 → 排队 → 执行 → 更新进度 → 保存结果”的完整链路。

### 实施步骤

1. 创建 `diagnostic_tasks` 表。
2. 定义任务状态：

   ```text
   CREATED、QUEUED、RUNNING、SEARCHING、VERIFYING、REPORTING、COMPLETED、FAILED、CANCELLED
   ```

3. 实现 `POST /api/diagnoses`。
4. 实现 `GET /api/diagnoses/{task_id}`。
5. 将任务发送到 Redis 队列。
6. 实现 Worker 消费任务并更新状态。
7. 实现 `GET /api/diagnoses/{task_id}/events`，通过 SSE 推送进度。
8. 实现取消任务和失败重试。
9. 保存任务执行日志和错误信息。

### 任务表

```sql
CREATE TABLE diagnostic_tasks (
    id VARCHAR(64) PRIMARY KEY,
    project VARCHAR(100) NOT NULL,
    question TEXT NOT NULL,
    status VARCHAR(32) NOT NULL,
    progress INT DEFAULT 0,
    current_step VARCHAR(255),
    result JSONB,
    error TEXT,
    retry_count INT DEFAULT 0,
    created_by VARCHAR(100),
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL
);
```

### 验收标准

- 创建请求在 2 秒内返回 `task_id`；
- Worker 可以异步执行任务；
- SSE 可以看到状态和进度变化；
- Worker 异常后任务进入 `FAILED`；
- 重试不会产生重复结果；
- 取消任务后不再执行新的步骤。

---

## 6. 阶段 2：代码检索 MVP

### 目标

让 Agent 能够针对 PHP 仓库进行精确搜索和文件读取，并输出真实文件路径和行号。

### 实施步骤

1. 配置项目仓库白名单和目录白名单。
2. 封装 `ripgrep` 代码搜索工具。
3. 封装文件读取工具。
4. 限制搜索结果数量、文件大小和执行时间。
5. 实现 Agent 工具调用循环。
6. 编写问题理解 Prompt，提取项目、业务对象、操作、错误现象和关键词。
7. 建立证据对象结构。
8. 创建 `diagnostic_evidence` 表。
9. 实现基础报告生成器。
10. 对重复搜索结果去重。

### 工具接口

```json
{
  "name": "search_code",
  "parameters": {
    "query": "BD_GUILD_JOIN_DAY_LIMIT",
    "repository": "waky3",
    "path": "php/",
    "max_results": 20
  }
}
```

```json
{
  "name": "read_file",
  "parameters": {
    "file": "php/action/BDCenterControl.php",
    "start_line": 230,
    "end_line": 270
  }
}
```

### Agent 最小流程

```text
解析问题
  ↓
生成 3～8 个搜索关键词
  ↓
调用 search_code
  ↓
选择高相关文件
  ↓
调用 read_file
  ↓
整理代码证据
  ↓
生成初步结论
```

### 验收标准

- 能定位指定配置项、方法或错误码；
- 报告包含真实文件路径和行号；
- Agent 不得引用没有检索到的文件；
- 搜索工具不能访问白名单外的目录；
- 单个任务的搜索次数和上下文大小受到限制。

---

## 7. 阶段 3：代码结构和调用链理解

### 目标

从文本搜索升级为 PHP 类、方法、函数和调用关系分析。

### 实施步骤

1. 集成 Tree-sitter PHP 解析器。
2. 扫描 PHP 文件并提取 namespace、class、interface、trait、method、function、常量、配置项和 SQL。
3. 创建 `code_symbols` 表。
4. 建立符号到文件和行号的映射。
5. 提取方法调用、类引用和配置读取关系。
6. 实现 `find_symbol` 工具。
7. 实现 `find_references` 工具。
8. 实现 `trace_call_chain` 工具。
9. 将调用链注入诊断上下文。
10. 对解析失败文件单独记录，不阻断整个索引任务。

### 调用链优先级

```text
HTTP/API 入口
  ↓
Controller
  ↓
Service / 业务类
  ↓
Model / SQL
  ↓
配置读取
  ↓
错误码或异常返回
```

### 验收标准

- 可以定位类和方法定义；
- 可以找到主要调用方和被调用方；
- 可以输出 Controller 到 Model 的基本链路；
- 调用链节点都包含文件和行号；
- 单个 PHP 文件解析失败不会影响其他文件。

---

## 8. 阶段 4：数据库、配置和日志验证

### 目标

让 Agent 能够用实际数据验证代码分析出的候选原因。

### 实施步骤

1. 为每个项目配置只读数据库连接。
2. 创建数据库只读账号。
3. 实现 SQL 语法检查和只读白名单。
4. 设置查询超时、最大行数和最大结果大小。
5. 对手机号、Token、密码和用户信息脱敏。
6. 实现 `query_database` 工具。
7. 实现配置读取工具。
8. 接入日志搜索工具。
9. 保存工具调用、查询条件和结果摘要。
10. 在报告中区分代码、配置、数据库和日志证据。

### 数据库安全规则

- 只允许 `SELECT`；
- 禁止 `INSERT`、`UPDATE`、`DELETE`、`DROP`、`ALTER`；
- 禁止多语句执行；
- 禁止访问系统表；
- 所有参数使用参数绑定；
- 所有查询写入审计日志。

### 验收标准

- 任意写 SQL 都会被拒绝；
- 超时查询会自动终止；
- 敏感字段不会展示给模型和用户；
- 报告包含数据库验证条件；
- 工具调用可以追溯到具体任务。

---

## 9. 阶段 5：混合检索和知识库

### 目标

提高复杂问题的召回能力，复用历史诊断、业务规则和文档。

### 实施步骤

1. 将代码、配置、文档和历史报告统一建模。
2. 对内容清洗并按类、方法、配置块和文档段落切分。
3. 建立全文索引。
4. 建立向量索引。
5. 实现混合检索：

   ```text
   关键词召回
   + 符号召回
   + 向量召回
   + 结果去重
   + 相关性排序
   ```

6. 增加历史问题相似度检索。
7. 增加业务规则文档检索。
8. 为每条结果保存来源、版本和更新时间。
9. 防止历史错误结论直接覆盖当前证据。
10. 实现仓库变更后的增量索引。

### 建议索引对象

- PHP 代码片段；
- 配置文件；
- SQL 和表结构；
- API 文档；
- 业务规则；
- 历史故障报告；
- 发布记录和变更说明。

### 验收标准

- 关键词不完全匹配时仍能召回相关内容；
- 召回结果带有明确来源；
- 历史报告不能单独作为最终结论；
- 代码变更后可以增量更新索引；
- 索引失败不会影响线上诊断任务。

---

## 10. 阶段 6：前端和协作能力

### 目标

提供接近聊天机器人的交互体验。

### 实施步骤

1. 创建问题输入框。
2. 展示任务状态、当前步骤和进度。
3. 展示 Agent 调用工具的摘要。
4. 展示最终结论、置信度和不确定项。
5. 展示代码证据，支持文件路径和行号定位。
6. 展示调用链和相关配置。
7. 支持取消、重试和复制报告。
8. 支持历史任务搜索。
9. 增加人工反馈：正确、不准确、证据不足、转人工。
10. 支持导出 Markdown 报告。

### 报告展示顺序

```text
问题理解
→ 结论
→ 置信度
→ 已确认事实
→ 证据
→ 调用链
→ 影响范围
→ 修复建议
→ 未验证项
```

### 验收标准

- 用户可以从创建任务看到最终结果；
- 长任务不会被误认为无响应；
- 代码证据可定位到文件和行号；
- 失败任务显示失败原因；
- 报告可以复制或导出。

---

## 11. 阶段 7：安全、性能和生产上线

### 11.1 安全步骤

1. 接入用户身份认证和项目级权限。
2. 配置仓库、目录和环境白名单。
3. 生产数据库使用只读账号。
4. 增加 SQL 审计和工具调用审计。
5. 将代码注释、日志和数据库文本作为数据处理，不能覆盖系统规则。
6. 设置 Agent 最大步骤数和最大工具调用次数。
7. 设置工具超时和结果大小限制。
8. 对 API、SSE 和后台任务增加访问控制。
9. 对敏感字段统一脱敏。
10. 设置任务和证据保留周期。

### 11.2 性能步骤

1. 将代码索引任务与诊断任务分开部署。
2. 对仓库版本复用索引。
3. 对常用关键词和历史问题增加缓存。
4. 对工具结果做任务级缓存。
5. 长文件按行区间读取，不一次性传给模型。
6. 对耗时工具使用独立队列。
7. 监控任务耗时、工具调用次数、失败率和报告置信度。

### 11.3 上线前检查

- API 鉴权已开启；
- 生产数据库账号为只读；
- 仓库路径经过白名单校验；
- SQL 写操作测试均被拒绝；
- 敏感字段脱敏测试通过；
- Worker 崩溃后任务可以恢复或重试；
- 工具调用和任务日志可查询；
- 报告结论都有证据来源；
- 监控、告警、备份和数据保留策略已配置。

---

## 12. 测试实施步骤

### 12.1 单元测试

覆盖：

- 任务状态流转；
- 搜索参数校验；
- 文件路径白名单；
- SQL 只读校验；
- 敏感信息脱敏；
- 报告 JSON 结构；
- 调用链节点解析。

### 12.2 集成测试

覆盖：

- API 创建任务到 Worker 执行；
- Redis 队列消费；
- SSE 推送；
- 代码搜索和文件读取；
- 数据库查询和审计；
- 任务失败重试。

### 12.3 诊断准确性测试

准备一组已有明确答案的问题，验证：

- 是否找到正确入口；
- 是否找到关键配置；
- 是否建立正确调用链；
- 是否引用真实行号；
- 是否区分事实和推测；
- 是否能说明证据不足。

---

## 13. 每阶段交付流程

每个阶段统一执行：

```text
1. 明确阶段目标
2. 设计接口和数据结构
3. 实现最小功能
4. 编写单元测试
5. 编写集成测试
6. 接入日志和监控
7. 使用真实问题验证
8. 修复验收问题
9. 更新 README 和配置说明
10. 冻结阶段版本
```

## 14. 第一版建议范围

第一版优先实现：

```text
任务 API
+ Redis Worker
+ SSE 进度
+ ripgrep 代码搜索
+ 文件读取
+ Agent 工具调用循环
+ 带文件路径和行号的诊断报告
```

暂不实现：

- 自动修改代码；
- 自动提交 Git；
- 任意 SQL 执行；
- 复杂多仓库联动；
- 全量向量知识库；
- 自动发布线上配置。

## 15. 推荐执行顺序

```text
第 1 步：完成环境和项目初始化
第 2 步：完成异步任务闭环
第 3 步：接入精确代码搜索
第 4 步：实现 Agent 工具调用循环
第 5 步：输出结构化诊断报告
第 6 步：增加 SSE 实时进度
第 7 步：接入 PHP 符号和调用链分析
第 8 步：接入只读数据库和日志验证
第 9 步：增加混合检索和历史知识库
第 10 步：完成前端、权限、监控和上线检查
```

## 16. 最终验收标准

1. 用户输入问题后 2 秒内返回任务 ID。
2. 任务可以异步执行并显示进度。
3. Agent 可以搜索 PHP 代码并读取相关文件。
4. 报告包含准确的文件路径和行号。
5. 报告区分已确认事实、推测和未验证项。
6. 数据库工具无法执行写操作。
7. 每个结论都能关联至少一个证据。
8. 任务失败后能够重试。
9. 任务、证据和工具调用都可以审计。
10. 典型 PHP 业务问题能够输出可供开发人员继续处理的诊断报告。
