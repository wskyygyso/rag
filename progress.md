# PHP 代码诊断 RAgent 实施进度

## 文档用途

本文档用于记录 RAgent 项目的阶段性推进情况。

每次阶段性提交必须更新本文档，至少记录：

- 本次完成了什么；
- 变更了哪些文件或模块；
- 如何验证；
- 当前是否存在阻塞；
- 下一步准备做什么。

代码维护约定：所有新增或修改的文件、类、方法和函数，都必须同步补充或更新中文注释，说明职责、关键参数、返回值和异常边界。

状态定义：

- `未开始`：尚未进入该阶段；
- `进行中`：已有实现工作，但尚未完成验收；
- `已完成`：阶段目标和验收项均已完成；
- `阻塞`：需要外部依赖、权限或用户决策才能继续。

---

## 当前总体状态

| 项目 | 状态 |
|---|---|
| 当前阶段 | 阶段 2：代码检索 MVP |
| 总体进度 | 40% |
| 最近更新 | 2026-09-20 |
| 当前负责人 | Codex |
| 当前阻塞 | 无 |
| 下一步 | 进入阶段 3，建立 PHP 符号索引和调用链分析基础 |

## 阶段进度

| 阶段 | 内容 | 状态 | 进度 |
|---|---|---|---:|
| 0 | 环境和项目初始化 | 已完成 | 100% |
| 1 | 异步任务闭环 | 已完成 | 100% |
| 2 | 代码检索 MVP | 已完成 | 100% |
| 3 | 代码结构和调用链理解 | 未开始 | 0% |
| 4 | 数据库、配置和日志验证 | 未开始 | 0% |
| 5 | 混合检索和知识库 | 未开始 | 0% |
| 6 | 前端和协作能力 | 未开始 | 0% |
| 7 | 安全、性能和生产上线 | 未开始 | 0% |

---

## 阶段 0：环境和项目初始化

### 状态

已完成，100%。

### 已完成

- 完成 RAgent 总体实现方案；
- 完成分阶段实施步骤文档；
- 确定第一版采用异步任务、代码检索、证据链报告的总体方向；
- 确定首版只读访问，不自动修改代码、数据库和线上配置。
- 创建 Python 项目基础结构和依赖配置；
- 创建 FastAPI API、Celery Worker、Redis 和 PostgreSQL Compose 环境；
- 添加 `/` 和 `/health` 接口；
- 添加基础测试和 Docker 启动说明；
- 验证 Worker 可以消费健康检查任务。

### 已创建文件

- `implementation-plan.md`：总体实现方案；
- `implementation-steps.md`：分阶段实施步骤；
- `progress.md`：项目推进记录；
- `pyproject.toml`：Python 项目和依赖配置；
- `Dockerfile`：API/Worker 镜像构建配置；
- `docker-compose.yml`：API、Worker、Redis 和 PostgreSQL 编排配置；
- `app/`：FastAPI、配置、健康检查和 Worker 代码；
- `tests/`：阶段 0 基础测试；
- `.env.example`：环境变量模板；
- `README.md`：启动和测试说明。

### 待完成

- 阶段 0 无剩余交付项。

### 当前验证

- 已确认项目目录当前没有既有业务代码和项目框架；
- 已确认方案和步骤文档已经写入项目目录；
- `python3 -m compileall -q app tests` 通过；
- `.venv/bin/pytest -q` 通过，结果为 `1 passed`；
- `docker compose config` 通过；
- Docker Compose API、Worker、Redis 和 PostgreSQL 均启动成功；
- `GET /health` 返回 Redis 和 PostgreSQL 均为 `ok`；
- Worker 健康检查任务执行成功并返回 `{'status': 'ok'}`。

### 下一步

进入阶段 1，创建诊断任务表、任务创建/查询接口、Redis 队列任务和 SSE 进度推送。

---

## 阶段 1：异步任务闭环

### 状态

已完成，100%。

### 目标

实现“创建任务 → 排队 → 执行 → 更新进度 → 保存结果”的完整链路。

### 进入条件

- 阶段 0 完成；
- API、Worker、Redis 和 PostgreSQL 可以启动；
- 数据库迁移和基础测试可执行。

### 已完成

- 创建 `diagnostic_tasks` 表及对应迁移文件；
- 实现诊断任务创建和查询接口；
- 实现任务取消接口；
- 接入 Redis/Celery 异步 Worker；
- 实现任务状态、进度、结果和错误信息持久化；
- 实现 SSE 任务进度推送；
- 实现 Worker 对已取消任务的中止检查；
- 实现任务失败状态记录和 Celery 重试配置。

### 验收结果

- `POST /api/diagnoses` 创建任务成功；
- Worker 成功消费任务并将状态更新为 `COMPLETED`；
- `GET /api/diagnoses/{task_id}` 返回任务和占位报告；
- `GET /api/diagnoses/{task_id}/events` 成功推送 SSE 事件；
- Redis、PostgreSQL 和 API Compose 集成验证通过。

### 下一步准备

- 进入阶段 2：代码检索 MVP；
- 配置代码仓库白名单；
- 实现 `search_code` 和 `read_file` 工具；
- 将检索证据接入诊断报告；
- 增加 `diagnostic_evidence` 证据表持久化。

---

## 阶段 2：代码检索 MVP

### 状态

已完成，100%。

### 目标

支持 PHP 仓库的精确代码搜索、文件读取和带行号的诊断报告。

### 进入条件

- 阶段 1 完成；
- 异步任务和进度推送稳定可用。

### 已完成

- 增加固定 PHP 仓库样例集成测试；
- 增加搜索结果文件类型过滤；
- 增加搜索超时转换为业务异常；
- 完成代码仓库白名单、文件读取、代码检索和证据持久化闭环。

### 验收结果

- 代码工具测试覆盖 PHP 文件搜索、上下文读取和路径穿越防护；
- 搜索关键词、结果数量和文件扩展名均有边界限制；
- 搜索超时不会直接泄露底层异常；
- Worker 能将代码上下文写入诊断证据表。

### 下一步准备

- 进入阶段 3：PHP 符号索引和调用链分析；
- 解析类、方法、函数、配置项和 SQL；
- 建立符号定义与引用关系。

---

## 阶段 3：代码结构和调用链理解

### 状态

未开始，0%。

### 目标

解析 PHP 类、方法、函数、引用和调用链。

### 进入条件

- 阶段 2 可以稳定搜索和读取代码；
- 已有可用于验证的 PHP 示例仓库。

### 下一步准备

- 集成 Tree-sitter PHP；
- 建立符号索引；
- 实现 `find_symbol`、`find_references` 和 `trace_call_chain`；
- 为调用链节点记录文件和行号。

---

## 阶段 4：数据库、配置和日志验证

### 状态

未开始，0%。

### 目标

使用真实数据验证代码分析出的候选原因。

### 进入条件

- 阶段 3 已能找到代码入口和关键调用链；
- 已取得测试环境只读数据库和日志访问权限。

### 下一步准备

- 配置数据库只读账号；
- 实现 SQL 只读校验；
- 实现 `query_database`；
- 接入配置和日志查询；
- 增加敏感字段脱敏和审计。

---

## 阶段 5：混合检索和知识库

### 状态

未开始，0%。

### 目标

结合关键词、符号、向量和历史报告，提高复杂问题的召回能力。

### 进入条件

- 阶段 2 至阶段 4 已有稳定的代码和事实检索能力；
- 已收集可使用的业务文档和历史问题。

### 下一步准备

- 建立代码和文档分块流程；
- 建立全文索引和向量索引；
- 实现混合检索和结果排序；
- 增加历史问题召回；
- 实现仓库版本变更后的增量索引。

---

## 阶段 6：前端和协作能力

### 状态

未开始，0%。

### 目标

提供接近聊天机器人的任务进度、证据和报告展示能力。

### 进入条件

- API 和报告 JSON 格式已稳定；
- SSE 进度接口可用。

### 下一步准备

- 创建问题输入界面；
- 展示任务状态和执行步骤；
- 展示结论、证据、调用链和未验证项；
- 支持取消、重试、复制和导出报告；
- 增加人工反馈。

---

## 阶段 7：安全、性能和生产上线

### 状态

未开始，0%。

### 目标

满足生产环境的权限、安全、性能、监控和审计要求。

### 进入条件

- 核心诊断链路已完成；
- 已完成真实问题验证；
- 已明确生产环境的仓库、数据库和日志访问边界。

### 下一步准备

- 接入身份认证和项目级权限；
- 完成 SQL、路径和工具调用安全校验；
- 增加任务超时、限流、缓存和监控；
- 完成故障恢复、备份和告警；
- 执行上线前安全检查。

---

## 阶段性提交记录

### 2026-09-20：完成阶段 2 代码检索 MVP

#### 本次推进

- 增加固定 PHP 仓库样例，验证真实 PHP 文件检索和行号定位；
- 增加搜索结果扩展名过滤，避免无关二进制和依赖文件进入结果；
- 将搜索超时转换为可识别的 `CodeToolError`；
- 完成阶段 2 的代码仓库白名单、关键词搜索、文件读取、上下文提取和证据持久化闭环；
- 同步更新阶段进度，准备进入阶段 3。

#### 本次验证

- `.venv/bin/pytest -q` 通过，结果为 `5 passed`；
- Python 编译检查通过；
- `git diff --check` 通过；
- PHP 仓库样例成功定位 `BD_OP_FORBIDDEN` 所在文件和行号；
- 代码搜索边界和路径安全测试通过。

#### 当前状态

- 当前阶段：阶段 2 已完成，阶段 3 待开始；
- 阶段进度：阶段 2 为 100%；
- 总体进度：40%；
- 阻塞项：无。

#### 下一步

- 进入阶段 3，建立 PHP 符号索引；
- 解析类、方法、函数、配置项和 SQL；
- 建立符号定义、引用和调用链关系。

---

### 2026-09-20：补齐现有代码注释

#### 本次推进

- 为 `app/` 下现有模块增加模块职责说明；
- 为配置模型、仓库类、异常类和请求模型增加类注释；
- 为所有公开及关键内部方法、函数增加用途和边界说明；
- 为测试模块和测试函数增加测试意图注释；
- 在 README 和本进度文件中固定“新增或修改代码必须同步更新中文注释”的维护规则。

#### 本次验证

- 已检查现有 Python 模块、类、方法和测试函数的注释覆盖；
- `git diff --check` 通过；
- 下一步执行完整测试，确认注释变更未影响运行逻辑。

#### 当前状态

- 当前阶段：阶段 2：代码检索 MVP；
- 阶段进度：80%；
- 总体进度：36%；
- 阻塞项：无。

#### 下一步

- 执行完整测试和编译检查；
- 继续阶段 2 的真实 PHP 仓库集成验证；
- 后续新增或修改代码时同步维护中文注释。

---

### 2026-09-20：接入代码检索和证据持久化

#### 本次推进

- 创建 `app/code_tools.py`，实现代码仓库白名单、路径安全校验、关键词搜索和按行读取；
- 支持 `ripgrep`，未安装时自动回退到 Python 文本搜索；
- 增加搜索关键词提取和去重；
- 将代码搜索接入诊断 Worker；
- 增加检索命中上下文读取；
- 创建 `migrations/002_create_diagnostic_evidence.sql`；
- 在 Worker 中将代码证据写入 `diagnostic_evidence` 表；
- 增加仓库只读挂载和代码目录配置；
- 增加代码工具单元测试，测试总数达到 4 项。

#### 本次验证

- `.venv/bin/pytest -q` 通过，结果为 `4 passed`；
- Python 编译检查通过；
- `docker compose config` 通过；
- Compose 服务重建并启动成功；
- PostgreSQL 成功执行证据表迁移，表备注已生效；
- API 健康检查通过；
- 诊断任务能够进入代码搜索阶段并生成关键词、证据和告警字段；
- 未挂载项目仓库时，Worker 能安全返回“代码仓库不存在”告警，不会导致任务异常退出。

#### 当前状态

- 当前阶段：阶段 2：代码检索 MVP；
- 阶段进度：80%；
- 总体进度：36%；
- 阻塞项：无。

#### 下一步

- 增加真实 PHP 代码仓库的 Compose 集成测试；
- 完成阶段 2 验收；
- 进入阶段 3，接入 PHP 符号和调用链分析。

---

### 2026-09-20：补充诊断任务表 SQL 备注

#### 本次推进

- 为 `migrations/001_create_diagnostic_tasks.sql` 增加表级备注；
- 为任务 ID、项目、问题、环境、状态、进度、结果、错误、重试次数和时间字段增加中文备注；
- 明确任务状态枚举和进度百分比的含义，便于后续数据库维护和排查。

#### 本次验证

- 在 Compose PostgreSQL 中执行迁移文件成功；
- 使用 PostgreSQL 元数据查询确认表备注、状态字段备注和进度字段备注已生效；
- `git diff --check` 通过。

#### 当前状态

- 当前阶段：阶段 2：代码检索 MVP；
- 阶段进度：0%；
- 总体进度：25%；
- 阻塞项：无。

#### 下一步

- 配置代码仓库和目录白名单；
- 实现 `search_code` 和 `read_file` 工具。

---

### 2026-09-20：完成阶段 1 异步任务闭环

#### 本次推进

- 创建 `app/task_store.py`，实现诊断任务表初始化、创建、查询、更新和取消；
- 创建 `app/diagnosis.py`，实现任务创建、查询、取消和 SSE 事件接口；
- 扩展 `app/worker.py`，实现诊断任务异步消费、状态推进、占位报告和失败记录；
- 扩展 `app/main.py`，加入数据库连接池生命周期管理；
- 增加 `migrations/001_create_diagnostic_tasks.sql`；
- 将阶段 1 状态更新为已完成，并切换到阶段 2。

#### 本次验证

- `.venv/bin/pytest -q` 通过，结果为 `1 passed`；
- Python 编译检查通过；
- `docker compose config` 通过；
- Compose API、Worker、Redis 和 PostgreSQL 启动成功；
- 创建诊断任务成功并返回 `task_id`；
- Worker 成功将任务从 `QUEUED` 推进到 `COMPLETED`；
- 查询接口返回任务状态和占位报告；
- SSE 接口成功推送最终进度事件；
- Worker 日志确认 `run_diagnosis_task` 执行成功。

#### 当前状态

- 当前阶段：阶段 1 已完成，阶段 2 待开始；
- 阶段进度：阶段 1 为 100%；
- 总体进度：25%；
- 阻塞项：无。

#### 下一步

- 配置代码仓库和目录白名单；
- 实现 `search_code` 和 `read_file` 工具；
- 将真实代码证据接入 Agent 诊断报告。

---

### 2026-09-20：完成阶段 0 环境和项目初始化

#### 本次推进

- 创建 `pyproject.toml`，配置 FastAPI、Celery、Redis、PostgreSQL 和测试依赖；
- 创建 `Dockerfile` 和 `docker-compose.yml`；
- 创建 FastAPI 应用、配置模块、健康检查模块和 Celery Worker；
- 创建 `.env.example` 和本地 `.env` 配置；
- 创建基础测试和项目 README；
- 将 API、Worker、Redis 和 PostgreSQL 连接到同一 Compose 环境。

#### 本次验证

- Python 文件编译检查通过；
- `.venv/bin/pytest -q` 通过，结果为 `1 passed`；
- `docker compose config` 通过；
- Docker Compose 四个服务均成功启动；
- `GET /health` 返回整体 `ok`，Redis 和 PostgreSQL 均为 `ok`；
- Worker 健康检查任务执行成功并返回 `{'status': 'ok'}`。

#### 当前状态

- 当前阶段：阶段 0 已完成，阶段 1 待开始；
- 阶段进度：阶段 0 为 100%；
- 总体进度：12%；
- 阻塞项：无。

#### 下一步

- 创建 `diagnostic_tasks` 数据表；
- 实现诊断任务创建和查询 API；
- 将诊断任务发送到 Redis 队列；
- 实现 SSE 进度推送和任务失败重试。

---

### 2026-09-20：创建进度文档

#### 本次推进

- 创建项目进度记录文件；
- 建立阶段状态、进度、已完成事项和下一步的统一记录格式；
- 初始化 0 至 7 阶段的进度表；
- 将当前状态设置为阶段 0 进行中，整体进度为 0%。

#### 本次验证

- 确认进度文件已写入当前项目目录；
- 确认现阶段没有代码实现阻塞项。

#### 下一步

开始阶段 0：创建 Python 项目基础结构、配置文件、Docker Compose 和健康检查接口。

---

## 后续更新模板

后续每次阶段性提交，都在“阶段性提交记录”最上方追加一条记录，并同步修改“当前总体状态”和对应阶段状态。

```markdown
### YYYY-MM-DD：阶段名称 / 提交主题

#### 本次推进

- 完成了什么；
- 新增或修改了哪些文件；
- 解决了什么问题。

#### 本次验证

- 执行了哪些检查或测试；
- 检查结果是什么；
- 是否存在已知问题。

#### 当前状态

- 当前阶段：
- 阶段进度：
- 总体进度：
- 阻塞项：无 / 具体描述。

#### 下一步

- 下一次准备完成什么；
- 需要哪些前置条件或外部依赖。
```
